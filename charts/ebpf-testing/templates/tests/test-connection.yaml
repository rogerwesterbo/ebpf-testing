apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ebpf-testing.fullname" . }}-test-connection"
  labels:
    {{- include "ebpf-testing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ebpf-testing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
